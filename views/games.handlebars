<section class="card">
  <a href="/post/{{user_id}}">
    <header>{{title}}</header>

    <div class="card-body">
      {{!-- <img src="/images/{{filename}}" alt="{{description}}"> --}}
      <p>By: {{name}}</p>
    </div>
  </a>
</section>